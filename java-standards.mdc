---
description: Java 通用编码规范，覆盖命名、异常、日志、注释、性能、安全
globs: **/*.java
alwaysApply: false
---

# Java 编码规范

## 一、命名规范

### 包名
全小写：`com.ydd.system.{层级}`

### 类名（大驼峰）
- Controller：`{业务}Controller`（如 `SysConfigController`）
- Service 接口：`I{业务}Service`（如 `ISysConfigService`）
- Service 实现：`{业务}ServiceImpl`（如 `SysConfigServiceImpl`）
- Mapper：`{业务}Mapper`（如 `SysDictDataMapper`）
- Entity：`{业务}Entity` 或直接业务名（如 `SysConfig`、`UserActivityEntity`）
- DTO：`{业务}DTO` / `{业务}BaseDTO`
- VO：`{业务}VO`
- Request：`{业务}Request`
- Query：`{业务}Query`

### 方法名（小驼峰）
- Controller 层：`list` / `getInfo` / `add` / `edit` / `remove` / `export`
- Service 层：`select*` / `insert*` / `update*` / `delete*` / `check*`
- Mapper 层：与 Service 对应

### 常量
全大写下划线分隔：`MAX_RETRY_COUNT`、`DEFAULT_PAGE_SIZE`

```java
// ❌ 错误
public class sysConfigController {}
public interface SysConfigService {}
public void GetUserInfo() {}

// ✅ 正确
public class SysConfigController {}
public interface ISysConfigService {}
public void getUserInfo() {}
```

## 二、异常处理

### 规则
- Controller 不捕获异常，由全局异常处理器统一处理
- 业务异常统一使用 `ServiceException`
- **禁止空 catch**（吞掉异常）
- 禁止在循环中抛异常
- 可校验的参数使用 `@Validated`，不要手写 if-else

```java
// ❌ 错误 - 空 catch
try {
    userService.updateUser(user);
} catch (Exception e) {
}

// ❌ 错误 - Controller 捕获异常
@PostMapping("/add")
public AjaxResult add(@RequestBody User user) {
    try {
        return toAjax(userService.insertUser(user));
    } catch (Exception e) {
        return AjaxResult.error(e.getMessage());
    }
}

// ✅ 正确 - Service 层抛业务异常
if (StrUtil.isBlank(data.getDictType())) {
    throw new ServiceException("字典类型不能为空");
}

// ✅ 正确 - Controller 不捕获，交给全局处理
@PostMapping("/add")
public AjaxResult add(@Validated @RequestBody User user) {
    return toAjax(userService.insertUser(user));
}
```

## 三、日志规范

### 规则
- 使用 SLF4J（`@Slf4j` 或 `LoggerFactory.getLogger`）
- 使用 `{}` 占位符，**禁止字符串拼接**
- **禁止** `System.out.println` 和 `e.printStackTrace()`
- 敏感信息（密码、手机号、身份证）**必须脱敏**
- Controller 写操作使用 `@Log` 注解记录操作日志

### 日志级别
- `ERROR`：系统异常、不可恢复的错误
- `WARN`：业务异常、可预期的异常情况
- `INFO`：关键业务节点（如用户登录、订单创建）
- `DEBUG`：调试信息，生产环境不输出

```java
// ❌ 错误
System.out.println("用户ID：" + userId);
log.info("处理用户：" + userId + "，机构：" + orgId);
e.printStackTrace();

// ✅ 正确
log.info("处理用户：userId={}, orgId={}", userId, orgId);
log.error("用户操作失败：userId={}", userId, e);
```

## 四、注释文档

### 规则
- 类级别：必须有 Javadoc，含 `@author`
- Service 接口方法：必须有 `@param` 和 `@return`
- Controller 方法：使用 `@ApiOperation` 描述
- 复杂业务逻辑：加行内注释解释**意图**
- **禁止**无意义注释（如"获取ID"注释在 `getId()` 上）

```java
// ❌ 错误 - 无意义注释
/** 获取用户ID */
public Long getUserId() { return userId; }

// ✅ 正确 - 类 Javadoc
/**
 * 字典业务层处理
 *
 * @author ydd
 */
@Service
public class SysDictDataServiceImpl implements ISysDictDataService {

// ✅ 正确 - 接口方法 Javadoc
/**
 * 根据字典类型和键值查询字典数据
 *
 * @param dictType  字典类型
 * @param dictValue 字典键值
 * @return 字典数据
 */
SysDictData selectDictLabel(String dictType, String dictValue);
```

## 五、性能规范

### 规则
- **禁止循环内执行数据库查询**（N+1 问题），改用批量查询
- 大批量数据操作使用 `saveBatch` / `updateBatchById`
- 集合初始化指定容量：`new ArrayList<>(size)` / `new HashMap<>(size)`
- 字符串拼接使用 `StringBuilder`（循环场景）
- 分页查询**必须**带分页参数，禁止全表查询
- 合理使用缓存，避免频繁查询数据库

```java
// ❌ 错误 - 循环内查询
for (Long userId : userIds) {
    SysUser user = userMapper.selectById(userId);
    // ...
}

// ✅ 正确 - 批量查询
List<SysUser> users = userMapper.selectBatchIds(userIds);
Map<Long, SysUser> userMap = users.stream()
    .collect(Collectors.toMap(SysUser::getUserId, Function.identity()));

// ❌ 错误 - 未指定容量
List<String> names = new ArrayList<>();

// ✅ 正确
List<String> names = new ArrayList<>(userList.size());
```

## 六、安全编码

### 规则
- **禁止**硬编码密码、密钥、token
- 敏感数据（密码、手机号、身份证号）**禁止**明文写入日志
- API 接口必须有权限控制
- 文件上传必须校验文件类型（用 Tika 检测）和大小
- 输入输出过滤防 XSS

```java
// ❌ 错误 - 硬编码密码
String password = "admin123";

// ❌ 错误 - 日志泄露敏感信息
log.info("用户手机号：{}", user.getPhone());

// ✅ 正确 - 从配置读取
@Value("${sys.user.initPassword}")
private String initPassword;

// ✅ 正确 - 脱敏
log.info("用户手机号：{}", StrUtil.hide(user.getPhone(), 3, 7));
```
